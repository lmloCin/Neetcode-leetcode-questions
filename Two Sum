class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        for n in range(len(nums)): 
            i = n+1
            while i != len(nums):
                if (nums[n] + nums[i]) == target:
                    return [n, i]
                else:
                    i = i + 1
